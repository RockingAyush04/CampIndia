# Environment Variables Template
# Copy this file to .env and update the values

# Database Configuration
# For local MongoDB (default):
# MONGODB_URI=mongodb://127.0.0.1:27017/yelp-camp

# For MongoDB Atlas (production):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/campindia

# Server Configuration
# PORT=3000

# Instructions:
# 1. For local development: Just run `npm start` (uses default values)
# 2. For production: Set MONGODB_URI to your MongoDB Atlas connection string
# 3. Make sure to install MongoDB locally or use MongoDB Atlas for cloud database
